.chatInfo {
  border-left: 2px solid var(--colorTuna);

  &__top {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 20px 24px;
    border-bottom: 2px solid var(--colorTuna);
  }

  &__topTitle {
    font-weight: 600;
    font-size: 16px;
    line-height: 1.5;
  }
}
